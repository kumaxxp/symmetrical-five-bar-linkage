``` plantuml

@startuml
class ForwardKinematics {
  +__init__(Yb, l, b, m, e)
  +set_angles(theta1, theta2)
  +compute_forward_kinematics()
  +calculate()
}

class ExtendedKinematics {
  -f: float
  -F: tuple
  -thetaF: float
  -transformer: Transformation2D
  -transformed_points: dict
  -original_points: dict
  -link_colors: dict
  -point_colors: dict
  +__init__(Yb, l, b, m, e, f)
  +set_angles(theta1, theta2, thetaF)
  +compute_forward_kinematics()
  -calculate_F()
  +calculate()
  -format_result()
  +apply_transformation(origin, angle, translation)
  +get_transformed_points()
  +get_original_points()
  +get_link_points()
  +get_link_colors()
  +get_point_colors()
  +get_lightened_colors(amount)
  +angle_between_vectors(v1_start, v1_end, v2_start, v2_end)
}

class Transformation2D {
  -origin
  -angle: float
  -translation: tuple
  +__init__(origin, angle, translation)
  +transform(point)
}

ForwardKinematics <|-- ExtendedKinematics
ExtendedKinematics --> Transformation2D
@enduml
