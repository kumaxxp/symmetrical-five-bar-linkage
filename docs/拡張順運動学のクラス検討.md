```plantuml

@startuml
class ForwardKinematics {
  +__init__(Yb, l, b, m, e)
  +set_angles(theta1, theta2)
  +compute_forward_kinematics()
  +calculate()
}

class ExtendedKinematics {
  -f: float
  -F: tuple
  -thetaF: float
  -transformer: Transformation2D
  -transformed_points: dict
  -original_points: dict
  -link_colors: dict
  -point_colors: dict
  +__init__(Yb, l, b, m, e, f)
  +set_angles(theta1, theta2, thetaF)
  +compute_forward_kinematics()
  -calculate_F()
  +calculate()
  -format_result()
  +apply_transformation(transformer)
  +get_transformed_points()
  +get_original_points()
  +get_link_points()
  +get_link_colors()
  +get_point_colors()
  +get_lightened_colors(amount)
}

class Transformation2D {
  -origin: tuple
  -angle: float
  -translation: tuple
  +__init__(origin, angle, translationtransform(point)
}

class Hip {
  -left_leg: ExtendedKinematics
  -right_leg: ExtendedKinematics
  -hip_position: tuple
  -hip_angle: float
  +__init__(left_leg, right_leg)
  +set_hip_position(x, y)
  +set_hip_angle(angle)
  +compute_leg_positions()
  +get_transformed_points()
}

class KinematicsApp {
  -hip: Hip
  -canvas: Canvas
  -sliders: dict
  +__init__()
  +setup_ui()
  +update_plot()
  +draw_kinematics()
}

ForwardKinematics <|-- ExtendedKinematics
ExtendedKinematics --> Transformation2D
Hip "1" *-- "2" ExtendedKinematics
KinematicsApp "1" *-- "1" Hip
@enduml
